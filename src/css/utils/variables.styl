// -------------------
// Breakpoints
// -------------------
$mq_vars = {
    bp-1: '(min-width: 25em)'
    bp-2: '(min-width: 40em)'
    bp-3: '(min-width: 50em)'
    bp-4: '(min-width: 60em)'
		bp-desktop: '(min-width: 60em)'
}

// -------------------
// Typefaces
// -------------------
$canela = 'Canela Web'
$canela-hash = 'canela'
$publico = 'Publico Text Web'
$publico-hash = 'publico'
$atlas = 'Atlas Grotesk Web'
$atlas-hash = 'atlas'

$sans-fb = Helvetica, Arial, sans-serif
$sans = $atlas, $sans-fb
$serif-fb = Georgia, Times, serif
$serif = $publico, $serif-fb
$serif-hed-fb = 'Times New Roman', serif
$mono = Menlo, Consolas, Monaco, 'Courier New', monospace

// z-index
$z-index = {
    bottom: -100
    middle: 0
    top: 100
    overlay: 1000
}

// Color
$red = #e83643
$yellow = #ffee15
$green = #b1eb00
$blue = #0688f2
$purple = #592bd8
$blue-light = lighten(#cde7fc, 2)

// day mode
$off-black = darken($blue, 36)
$gray-dark = darken($blue, 24)
$gray = darken($blue, 18)
$gray-light = lighten($blue, 24)
$off-white = lighten($blue, 36)
$bg = $blue-light

$btn-color = $yellow
// night mode
// $off-black = lighten($purple, 36)
// $gray-dark = lighten($purple, 24)
// $gray = lighten($purple, 18)
// $gray-light = darken($purple, 24)
// $off-white = darken($purple, 36)

// $bg = darken($purple, 36)


$fg = $off-black
$primary= $off-black



$column-width = 40rem

$type-size = {
    xxx-small: {
        size: 10px,
        line-height: 1.5,
    }
    xx-small: {
        size: 11px,
        line-height: 1.5,
    }
    x-small: {
        size: 12px,
        line-height: 1.5
    }
    small: {
        size: 14px,
        line-height: 1.5    
    }
    root: {
        size: 16px,
        line-height: 1.7
    }
    medium: {
        size: 17px,
        line-height: 1.4
    }
    large: {
        size: 22px,
        line-height: 1.5
    }
    x-large: {
        size: 32px,
        line-height: 1.5
    }
    xx-large: {
        size: 40px,
        line-height: 1.2
    }
    xxx-large: {
        size: 48px,
        line-height: 1.2
    }
    xxxx-large: {
        size: 64px,
        line-height: 1.2
    }
}

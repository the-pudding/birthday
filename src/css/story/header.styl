header
	z-index set-index('overlay')
	position absolute
	top 0
	left 0
	width 100%
	opacity 0
	transition opacity 500ms ease-in-out
	&.is-visible
		opacity 1

	a.logo
		display block
		border none
		max-width 9rem
		margin 1rem auto

		svg
			fill $off-black
			display block
			width 100%
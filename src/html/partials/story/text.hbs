{{#each copy.steps}}
<div class='step text__step text__step--{{id}} {{class}}' data-id={{id}}>
	{{#if intro}}
		{{> story/intro}}
	{{else if appendix}}
		 {{> story/appendix}}
	{{else}}
	<p>{{{text}}}</p>
	{{/if}}
</div>
{{/each}}